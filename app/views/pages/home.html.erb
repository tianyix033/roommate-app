<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RoomMate · Coming Soon</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              midnight: "#0b1021",
              lavender: "#d8d6ff",
              mint: "#9ef0d4",
              amber: "#f6c177"
            },
            keyframes: {
              floaty: {
                "0%, 100%": { transform: "translateY(0)" },
                "50%": { transform: "translateY(-10px)" }
              },
              stroll: {
                "0%": { transform: "translateX(-10%)" },
                "50%": { transform: "translateX(55%)" },
                "100%": { transform: "translateX(120%)" }
              },
              sparkle: {
                "0%, 100%": { opacity: 0.2, transform: "translateY(0)" },
                "50%": { opacity: 1, transform: "translateY(-8px)" }
              }
            },
            animation: {
              floaty: "floaty 3.2s ease-in-out infinite",
              stroll: "stroll 16s linear infinite",
              sparkle: "sparkle 2.4s ease-in-out infinite"
            }
          }
        }
      };
    </script>
    <style>
      .cat-shadow {
        filter: drop-shadow(0 10px 18px rgba(0, 0, 0, 0.24));
      }
    </style>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-midnight via-slate-900 to-slate-950 text-lavender">
    <div class="relative isolate overflow-hidden">
      <div class="absolute inset-0 opacity-30 pointer-events-none">
        <div class="absolute -left-10 top-10 h-64 w-64 rounded-full bg-mint blur-3xl"></div>
        <div class="absolute right-0 bottom-0 h-72 w-72 rounded-full bg-amber blur-3xl"></div>
      </div>

      <div class="relative mx-auto flex min-h-screen max-w-5xl flex-col gap-12 px-6 py-16 lg:flex-row lg:items-center lg:justify-between lg:py-24">
        <header class="max-w-2xl space-y-6">
          <span class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-semibold uppercase tracking-[0.2em] text-mint">
            WIP Landing
          </span>
          <h1 class="text-4xl font-semibold leading-tight text-white sm:text-5xl">
            RoomMate is stretching its paws.
          </h1>
          <p class="text-lg text-white/80">
            We’re crafting the full experience. For now, explore listings or check back as we roll out matching, profiles, and verified spaces.
          </p>
          <div class="flex flex-wrap gap-3">
            <a href="/listings" class="rounded-full bg-mint px-6 py-3 text-sm font-semibold text-slate-900 shadow-lg shadow-mint/40 transition hover:-translate-y-0.5 hover:shadow-xl hover:shadow-mint/50">View Listings</a>
            <a href="/auth/login" class="rounded-full border border-white/20 px-6 py-3 text-sm font-semibold text-white/90 transition hover:border-mint/70 hover:text-mint">Sign in</a>
          </div>
        </header>

        <div class="relative w-full max-w-lg">
          <div class="absolute -left-6 -top-6 h-24 w-24 rounded-full bg-amber/30 blur-2xl"></div>
          <div class="absolute -right-10 bottom-0 h-20 w-36 rounded-full bg-mint/40 blur-2xl"></div>

          <div class="relative rounded-3xl border border-white/10 bg-gradient-to-br from-white/10 via-white/5 to-white/10 p-6 shadow-2xl backdrop-blur">
            <div class="flex items-center justify-between gap-4">
              <div class="space-y-1">
                <p class="text-sm font-semibold text-white">GitHub Repo</p>
                <p class="text-xs uppercase tracking-[0.25em] text-mint">NYU · CS-UY 4513 · Team 5</p>
              </div>
              <div class="flex flex-col items-end gap-2">
                <a
                  href="https://github.com/NYU-CSE-Software-Engineering/cs-uy-4513-f25-team5/actions/workflows/ci.yml"
                  target="_blank"
                  rel="noreferrer"
                  class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1 text-[11px] font-semibold text-white transition hover:border-mint/60 hover:text-mint"
                >
                  <span class="h-2 w-2 rounded-full bg-mint animate-pulse"></span>
                  CI status
                </a>
              </div>
            </div>
            <div class="mt-6 overflow-hidden rounded-2xl border border-white/10 bg-white/5">
              <a href="https://github.com/NYU-CSE-Software-Engineering/cs-uy-4513-f25-team5" target="_blank" rel="noreferrer">
                <div class="relative">
                  <img
                    src="https://github-readme-stats.vercel.app/api/pin/?username=NYU-CSE-Software-Engineering&repo=cs-uy-4513-f25-team5&theme=tokyonight&border_radius=16&show_owner=true"
                    alt="GitHub repository card"
                    class="h-auto w-full"
                    loading="lazy"
                  />
                  <div class="absolute right-3 top-3 rounded-full border border-white/10 bg-slate-900/70 px-3 py-1 text-xs font-semibold text-mint backdrop-blur">
                    Click for full repo
                  </div>
                </div>
              </a>
            </div>
            <p class="mt-3 text-xs text-white/60 flex items-center gap-2">
              <span class="h-2 w-2 rounded-full bg-mint animate-pulse"></span>
              Live GitHub stats + CI badge link.
            </p>
          </div>

          <div class="mt-6 rounded-3xl border border-white/10 bg-white/5 p-6 shadow-xl backdrop-blur">
            <div class="mb-4 flex items-center justify-between">
              <div>
                <p class="text-sm font-semibold text-white">All routes</p>
                <p class="text-xs uppercase tracking-[0.25em] text-mint">Live from router</p>
              </div>
              <span class="rounded-full bg-white/10 px-3 py-1 text-[11px] font-semibold text-white/80">Auto-updating</span>
            </div>
            <div class="max-h-72 space-y-2 overflow-auto">
              <% @routes.each do |route| %>
                <div class="grid grid-cols-1 gap-2 rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white/80 md:grid-cols-[80px_1fr_1fr]">
                  <div class="flex items-center gap-2">
                    <span class="rounded-full bg-white/10 px-2 py-1 text-[11px] font-semibold text-mint"><%= route[:verb] %></span>
                    <% if route[:name].present? %>
                      <span class="text-[11px] uppercase tracking-wide text-white/50"><%= route[:name] %></span>
                    <% end %>
                  </div>
                  <div class="text-white font-semibold break-all"><%= route[:path] %></div>
                  <div class="text-white/70">
                    <span class="font-semibold text-white/90"><%= route[:controller] %></span>
                    <span class="text-white/50">#</span>
                    <span class="text-white/80"><%= route[:action] %></span>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>

    </div>
  </body>
</html>
