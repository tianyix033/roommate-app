<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard · RoomMate</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              black: "#000000",
              white: "#ffffff",
              gray: {
                50: "#fafafa",
                100: "#f5f5f5",
                200: "#e5e5e5",
                300: "#d4d4d4",
                400: "#a3a3a3",
                500: "#737373",
                600: "#525252",
                700: "#404040",
                800: "#262626",
                900: "#171717",
                950: "#0a0a0a"
              }
            },
            fontFamily: {
              syne: ['Syne', 'sans-serif']
            },
            keyframes: {
              fadeIn: {
                "0%": { opacity: 0, transform: "translateY(20px)" },
                "100%": { opacity: 1, transform: "translateY(0)" }
              }
            },
            animation: {
              fadeIn: "fadeIn 0.6s ease-out forwards"
            }
          }
        }
      };
    </script>
    <style>
      .glass-card {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.08);
      }
      
      .hover-lift {
        transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
      }
      
      .hover-lift:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        border-color: rgba(255, 255, 255, 0.15);
      }
    </style>
  </head>
  <body class="min-h-screen bg-black text-white font-syne antialiased">
    <!-- Subtle grid background -->
    <div class="fixed inset-0 opacity-[0.02]" style="background-image: url('data:image/svg+xml,%3Csvg width=&quot;60&quot; height=&quot;60&quot; viewBox=&quot;0 0 60 60&quot; xmlns=&quot;http://www.w3.org/2000/svg&quot;%3E%3Cg fill=&quot;none&quot; fill-rule=&quot;evenodd&quot;%3E%3Cg fill=&quot;%23ffffff&quot; fill-opacity=&quot;1&quot;%3E%3Cpath d=&quot;M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z&quot;/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    
    <!-- Gradient orbs -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <div class="absolute -top-40 -right-40 w-80 h-80 bg-white/[0.02] rounded-full blur-3xl"></div>
      <div class="absolute -bottom-40 -left-40 w-96 h-96 bg-white/[0.015] rounded-full blur-3xl"></div>
    </div>

    <div class="relative z-10 min-h-screen flex flex-col">
      <!-- Top bar -->
      <nav class="fixed top-0 left-0 right-0 z-50 backdrop-blur-xl" style="background-color: oklch(20.5% 0 0 / 0.8);">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <a href="/dashboard" class="flex items-center">
            <span class="text-lg font-semibold tracking-tight transition-transform duration-200 hover:scale-110 inline-block">Room<span class="text-[#FFFC00]">Mate</span></span>
          </a>

          <div class="flex items-center gap-3">
            <a href="/dashboard" class="px-4 py-2 text-sm font-medium rounded-full bg-white/10 text-white border border-white/20 inline-flex items-center gap-1.5">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
              </svg>
              Home
            </a>
            <a href="/listings" class="px-4 py-2 text-sm font-medium rounded-full border border-white/20 text-white hover:bg-white/5 transition-colors duration-200">
              My Listings
            </a>
            <a href="/listings/search" class="px-4 py-2 text-sm font-medium rounded-full border border-white/20 text-white hover:bg-white/5 transition-colors duration-200 inline-flex items-center gap-1.5">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
              </svg>
              Search
            </a>
            <a href="/profile" class="px-4 py-2 text-sm font-medium rounded-full border border-white/20 text-white hover:bg-white/5 transition-colors duration-200">
              Profile
            </a>
            <%= button_to 'Logout', auth_logout_path, method: :post, 
                class: "px-4 py-2 text-sm font-medium rounded-full text-white hover:opacity-80 transition-opacity duration-200",
                style: "background-color: oklch(63.7% 0.237 25.331);" %>
          </div>
        </div>
      </nav>

      <!-- Main Content -->
      <main class="flex-1 pt-28 pb-20 px-6">
        <div class="max-w-6xl mx-auto">
          <% if notice.present? %>
            <div id="flash-notice" class="mb-6 px-4 py-3 rounded-xl bg-green-500/10 border border-green-500/20 text-green-400 text-sm opacity-0 animate-fadeIn transition-opacity duration-500">
              <%= notice %>
            </div>
            <script>
              setTimeout(() => {
                const notice = document.getElementById('flash-notice');
                if (notice) {
                  notice.style.opacity = '0';
                  setTimeout(() => notice.remove(), 500);
                }
              }, 3000);
            </script>
          <% end %>

          <% if alert.present? %>
            <div class="mb-6 px-4 py-3 rounded-xl bg-red-500/10 border border-red-500/20 text-red-400 text-sm opacity-0 animate-fadeIn">
              <%= alert %>
            </div>
          <% end %>

          <!-- Welcome Header -->
          <div class="mb-10 opacity-0 animate-fadeIn">
            <h1 class="text-4xl md:text-5xl font-bold tracking-tight mb-4">
              Welcome back, <span class="text-[#FFFC00]"><%= current_user&.display_name.presence || current_user&.email&.split('@')&.first || "Guest" %></span>
            </h1>
            <p class="text-xl text-gray-400">Access your profile and find compatible roommates from here.</p>
          </div>

          <!-- Quick Actions -->
          <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
            <a href="/listings" class="glass-card rounded-2xl p-6 hover-lift opacity-0 animate-fadeIn text-center" style="animation-delay: 0.1s;">
              <div class="w-12 h-12 rounded-xl bg-white/5 flex items-center justify-center mb-4 mx-auto">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                </svg>
              </div>
              <h3 class="text-lg font-semibold text-white mb-1">My Listings</h3>
              <p class="text-sm text-gray-400">Browse all available spaces</p>
            </a>

            <a href="/listings/search" class="glass-card rounded-2xl p-6 hover-lift opacity-0 animate-fadeIn text-center" style="animation-delay: 0.15s;">
              <div class="w-12 h-12 rounded-xl bg-white/5 flex items-center justify-center mb-4 mx-auto">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
              </div>
              <h3 class="text-lg font-semibold text-white mb-1">Search Listings</h3>
              <p class="text-sm text-gray-400">Find your perfect match</p>
            </a>

            <a href="/listings/new" class="glass-card rounded-2xl p-6 hover-lift opacity-0 animate-fadeIn text-center" style="animation-delay: 0.2s;">
              <div class="w-12 h-12 rounded-xl bg-white/5 flex items-center justify-center mb-4 mx-auto">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                </svg>
              </div>
              <h3 class="text-lg font-semibold text-white mb-1">Create Listing</h3>
              <p class="text-sm text-gray-400">Share your space</p>
            </a>

            <a href="/profile" class="glass-card rounded-2xl p-6 hover-lift opacity-0 animate-fadeIn text-center" style="animation-delay: 0.25s;">
              <% if current_user&.avatar.present? %>
                <div class="w-12 h-12 rounded-full overflow-hidden mb-4 mx-auto">
                  <img src="<%= current_user.avatar.data_uri %>" alt="Profile picture" class="w-full h-full object-cover"/>
                </div>
              <% else %>
                <div class="w-12 h-12 rounded-full bg-white/5 flex items-center justify-center mb-4 mx-auto">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                  </svg>
                </div>
              <% end %>
              <h3 class="text-lg font-semibold text-white mb-1">My Profile</h3>
              <p class="text-sm text-gray-400">Manage your account</p>
            </a>
          </div>

          <!-- User Info Card -->
          <div class="glass-card rounded-2xl p-8 opacity-0 animate-fadeIn" style="animation-delay: 0.3s;">
            <h2 class="text-2xl font-bold mb-6">Your Account</h2>
            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <p class="text-sm text-gray-400 mb-1">Email</p>
                <p class="text-lg text-white"><%= current_user&.email || "Not set" %></p>
              </div>
              <div>
                <p class="text-sm text-gray-400 mb-1">Display Name</p>
                <p class="text-lg text-white"><%= current_user&.display_name.presence || "Not set" %></p>
              </div>
              <div>
                <p class="text-sm text-gray-400 mb-1">Account Type</p>
                <p class="text-lg text-white">
                  <% if current_user&.admin? %>
                    <span class="inline-flex items-center gap-2">
                      <span class="px-2 py-1 text-xs font-semibold rounded-full bg-[#FFFC00]/20 text-[#FFFC00]">Admin</span>
                    </span>
                  <% else %>
                    <span class="text-gray-300">Regular User</span>
                  <% end %>
                </p>
              </div>
              <div>
                <p class="text-sm text-gray-400 mb-1">Member Since</p>
                <p class="text-lg text-white"><%= current_user&.created_at&.strftime("%B %d, %Y") || "Unknown" %></p>
              </div>
            </div>
          </div>

          <% if current_user&.admin? %>
            <!-- Admin Section -->
            <div class="mt-8 glass-card rounded-2xl p-8 border-[#FFFC00]/20 opacity-0 animate-fadeIn" style="animation-delay: 0.4s;">
              <div class="flex items-center gap-3 mb-6">
                <span class="px-3 py-1 text-xs font-semibold rounded-full bg-[#FFFC00]/20 text-[#FFFC00]">Admin</span>
                <h2 class="text-2xl font-bold">Admin Tools</h2>
              </div>
              <div class="grid md:grid-cols-2 gap-4">
                <a href="/verification_requests" class="flex items-center gap-4 p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors">
                  <div class="w-10 h-10 rounded-lg bg-green-500/20 flex items-center justify-center">
                    <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                  </div>
                  <div>
                    <p class="font-semibold text-white">Verify Listings</p>
                    <p class="text-sm text-gray-400">Review and approve listings</p>
                  </div>
                </a>
                <a href="/matches" class="flex items-center gap-4 p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors">
                  <div class="w-10 h-10 rounded-lg bg-blue-500/20 flex items-center justify-center">
                    <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                  </div>
                  <div>
                    <p class="font-semibold text-white">Manage Matches</p>
                    <p class="text-sm text-gray-400">View user matches</p>
                  </div>
                </a>
              </div>
            </div>
          <% end %>
        </div>
      </main>

      <!-- Footer -->
      <footer class="shrink-0 py-4 px-6 bg-black">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-gray-600">
          <span>Room<span class="text-[#FFFC00]">Mate</span> © 2025</span>
          <div class="flex items-center gap-6">
            <a href="/" class="hover:text-white transition-colors">Home</a>
            <a href="/listings" class="hover:text-white transition-colors">Listings</a>
            <a href="https://github.com/NYU-CSE-Software-Engineering/cs-uy-4513-f25-team5" target="_blank" class="hover:text-white transition-colors">GitHub</a>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
