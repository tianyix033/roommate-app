<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Listing · RoomMate</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              outfit: ['Outfit', 'sans-serif']
            }
          }
        }
      };
    </script>
    <style>
      .card {
        background: rgba(0, 0, 0, 0.6);
        border: 2px solid rgba(255, 255, 255, 0.3);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }
      .card:hover {
        transform: translateY(-8px) scale(1.02);
        border-color: rgba(255, 255, 255, 0.6);
        background: rgba(0, 0, 0, 0.9);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(255, 255, 255, 0.1);
      }
      .btn-pop {
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      }
      .btn-pop:hover {
        transform: scale(1.05);
      }
      .btn-pop:active {
        transform: scale(0.98);
      }
      .fade-in {
        animation: fadeIn 0.6s ease-in-out;
      }
      .fade-in-delay-1 {
        animation: fadeIn 0.6s ease-in-out 0.1s both;
      }
      .fade-in-delay-2 {
        animation: fadeIn 0.6s ease-in-out 0.2s both;
      }
      input[type="text"],
      input[type="number"],
      input[type="email"],
      textarea {
        background: rgba(0, 0, 0, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: white;
      }
      input[type="text"]:focus,
      input[type="number"]:focus,
      input[type="email"]:focus,
      textarea:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.5);
        background: rgba(0, 0, 0, 0.6);
      }
      input[type="text"]::placeholder,
      input[type="number"]::placeholder,
      input[type="email"]::placeholder,
      textarea::placeholder {
        color: rgba(255, 255, 255, 0.4);
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body class="min-h-screen bg-black font-outfit text-white">
    <!-- Navigation -->
    <nav style="background-color: oklch(26.9% 0 0);">
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <a href="/" class="text-2xl font-bold text-white">Room<span class="text-[#FFFC00]">Mate</span></a>
          <div class="flex items-center gap-4">
            <a href="/dashboard" class="text-white/60 hover:text-white transition-colors text-sm">Dashboard</a>
            <a href="/listings" class="text-white/60 hover:text-white transition-colors text-sm">Browse Listings</a>
            <%= button_to auth_logout_path, method: :post, class: "btn-pop flex items-center gap-2 px-4 py-2 rounded-lg bg-white text-black hover:bg-white/90 text-sm cursor-pointer font-medium" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
              </svg>
              <span>Log out</span>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="max-w-4xl mx-auto px-6 py-12">
      <!-- Header -->
      <div class="mb-8 fade-in">
        <h1 class="text-4xl font-bold text-white mb-4">Edit Listing</h1>
        <p class="text-white/60 text-lg">Update your listing information</p>
      </div>

      <!-- Error Messages -->
      <% if @listing.errors.any? %>
        <div class="mb-8 rounded-xl bg-red-500/20 border border-red-500/30 px-5 py-4 fade-in">
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 shrink-0 text-red-300 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <div class="flex-1">
              <h2 class="text-red-300 font-semibold mb-2">
                <%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:
              </h2>
              <ul class="list-disc list-inside space-y-1 text-red-200 text-sm">
                <% @listing.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Form Card -->
      <div class="card rounded-2xl p-8 fade-in-delay-1">
        <%= form_with(model: @listing, local: true, class: "space-y-6") do |form| %>
          <!-- Title -->
          <div>
            <%= form.label :title, 'Title', class: "block text-sm font-semibold text-white/60 mb-2 uppercase tracking-wide" %>
            <%= form.text_field :title, placeholder: "e.g., Cozy Room Near Campus", class: "w-full px-4 py-3 rounded-lg transition-colors" %>
          </div>

          <!-- Description -->
          <div>
            <%= form.label :description, 'Description', class: "block text-sm font-semibold text-white/60 mb-2 uppercase tracking-wide" %>
            <%= form.text_area :description, rows: 5, placeholder: "Describe your space, amenities, and what you're looking for in a roommate...", class: "w-full px-4 py-3 rounded-lg transition-colors resize-none" %>
          </div>

          <!-- Price and City -->
          <div class="grid gap-6 md:grid-cols-2">
            <div>
              <%= form.label :price, 'Monthly Rent', class: "block text-sm font-semibold text-white/60 mb-2 uppercase tracking-wide" %>
              <div class="relative">
                <span class="absolute left-4 top-1/2 -translate-y-1/2 text-white/60">$</span>
                <%= form.number_field :price, step: 0.01, placeholder: "0.00", class: "w-full px-4 py-3 pl-8 rounded-lg transition-colors" %>
              </div>
            </div>

            <div>
              <%= form.label :city, 'City', class: "block text-sm font-semibold text-white/60 mb-2 uppercase tracking-wide" %>
              <%= form.text_field :city, placeholder: "e.g., New York", class: "w-full px-4 py-3 rounded-lg transition-colors" %>
            </div>
          </div>

          <!-- Contact Email -->
          <div>
            <%= form.label :owner_email, "Contact Email", class: "block text-sm font-semibold text-white/60 mb-2 uppercase tracking-wide" %>
            <%= form.email_field :owner_email, placeholder: "your.email@example.com", class: "w-full px-4 py-3 rounded-lg transition-colors" %>
            <p class="mt-2 text-xs text-white/40">This email will be visible to potential roommates</p>
          </div>

          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row gap-4 pt-6 border-t border-white/10">
            <%= form.submit "Save Changes", class: "btn-pop flex-1 px-6 py-3 rounded-lg bg-white text-black hover:bg-white/90 font-medium cursor-pointer" %>
            <%= link_to 'View Listing', @listing, class: "btn-pop flex-1 text-center px-6 py-3 rounded-lg border border-white/10 text-white hover:bg-white/5" %>
            <%= link_to 'Cancel', listings_path, class: "btn-pop flex-1 text-center px-6 py-3 rounded-lg border border-white/10 text-white hover:bg-white/5" %>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
